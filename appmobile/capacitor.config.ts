import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.appmobile',
  appName: 'appmobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
